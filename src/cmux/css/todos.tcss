Screen {
    layout: vertical;
}

#session-header {
    height: 2;
    background: $primary-background;
    color: $text;
    padding: 0 1;
}

#task-list {
    height: 1fr;
    padding: 1;
    overflow-y: auto;
}
