Screen {
    layout: vertical;
}

#agent-summary {
    height: 1;
    background: $primary-background;
    padding: 0 1;
}

#waiting-message {
    height: 1fr;
    content-align: center middle;
    color: $text-muted;
}

#agent-table {
    height: 1fr;
}
